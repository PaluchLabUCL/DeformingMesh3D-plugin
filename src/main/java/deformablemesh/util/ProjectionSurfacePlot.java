package deformablemesh.util;

/**
 * For analyzing a mesh using a 2D projection. Values will be returned as x`, y`, z` coordinates where the ` denotes
 * that the coordinates are relative to rotated coordinates.
 *
 *
 */
public class ProjectionSurfacePlot {


}
